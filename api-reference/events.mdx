---
title: "Events"
description: "MCP tools для событий: список, детали, календарь, RSVP и предложения новых событий"
---

# Events

Events в EdgeLab покрывают просмотр расписания, участие и предложения новых событий.

## Доступные tools

| Tool | Доступ | Что делает |
|---|---|---|
| `list_events` | Edge+ | Возвращает список событий |
| `get_event` | Edge+ | Возвращает детали конкретного события |
| `get_event_calendar` | Edge+ | Даёт календарный артефакт или ссылку |
| `rsvp_event` | Edge+ | Записывает пользователя на событие |
| `cancel_rsvp` | Edge+ | Отменяет RSVP |
| `propose_event` | Pro/VIP | Отправляет предложение нового события |
| `list_proposals` | Pro/VIP | Возвращает свои предложения |

## `list_events`

Основной список событий.

Обычно здесь полезны фильтры:

- статус
- дата
- тип события
- лимит результатов

Используй этот tool в ежедневном cron, если агент должен следить за ближайшими сессиями.

## `get_event`

Возвращает карточку одного события:

- название
- описание
- время
- формат
- статусы участия
- связанные ссылки

## `get_event_calendar`

Нужен, если агент должен:

- отправить человеку ссылку на календарь
- сделать импорт в календарную систему
- синхронизировать напоминание

## `rsvp_event`

Записывает на событие.

### Пример вызова

```json
{
  "name": "rsvp_event",
  "arguments": {
    "event_id": 42
  }
}
```

## `cancel_rsvp`

Отменяет участие, если планы изменились.

## `propose_event`

Доступен только на Pro/VIP. Используется для пользовательских предложений новых событий и эфиров.

Типичный flow:

1. агент собирает идею
2. проверяет, нет ли похожего события
3. вызывает `propose_event`

## `list_proposals`

Помогает отслеживать, что уже было предложено и в каком статусе находится.

## Практика

Обычный сценарий:

```text
list_events -> get_event -> rsvp_event
```

Для продвинутых участников:

```text
list_events -> propose_event -> list_proposals
```
