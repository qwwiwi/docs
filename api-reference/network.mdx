---
title: "Network"
description: "MCP tools для сообщества и matching: список участников, поиск, совпадения и connect-запросы"
---

# Network

Нетворк в EdgeLab нужен, чтобы агент мог искать людей, находить совпадения и инициировать контакт.

## Доступные tools

| Tool | Доступ | Что делает |
|---|---|---|
| `list_members` | Edge+ | Возвращает список участников |
| `search_members` | Edge+ | Ищет участников по фильтрам и запросу |
| `get_matches` | Edge+ | Возвращает алгоритмические совпадения |
| `list_connect_requests` | Edge+ | Список входящих и исходящих connect-запросов |
| `send_connect_request` | Edge+ | Отправляет connect-запрос |
| `respond_connect_request` | Edge+ | Принимает или отклоняет запрос |

<Warning>
Network-инструменты доступны с тарифа Edge: поиск участников, matching и connect-запросы.
</Warning>

## `list_members`

Подходит для обзора каталога участников и построения собственных shortlist'ов.

## `search_members`

Используй, когда агент ищет людей под конкретную задачу:

- по навыкам
- по интересам
- по роли
- по городу
- по текстовому запросу

## `get_matches`

Это самый удобный старт, если не хочется строить поиск вручную. Платформа уже отдает наиболее релевантные совпадения по профилю.

## `list_connect_requests`

Нужен для мониторинга состояния диалогов:

- кто отправил запрос
- кому уже отправили
- что ещё pending

## `send_connect_request`

Отправляет инициирующий запрос.

### Пример вызова

```json
{
  "name": "send_connect_request",
  "arguments": {
    "target_user_id": "user_123",
    "message": "Вижу совпадение по AI automation и crypto. Давай познакомимся."
  }
}
```

## `respond_connect_request`

Нужен для ответа на входящий запрос.

Типичный аргумент:

- id запроса
- решение: accept или reject

## Практика

Хороший flow:

```text
get_matches -> search_members -> send_connect_request -> list_connect_requests -> respond_connect_request
```
