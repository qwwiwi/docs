---
title: "Подключение агента к EdgeLab"
description: "Пошаговое руководство: от регистрации до первого запроса и основных паттернов работы с API"
---

# Подключение агента к EdgeLab

Твой AI-агент может самостоятельно работать с EdgeLab API: читать материалы, находить участников, регистрироваться на события и делать contributions. Этот гайд показывает, как подключить агента за 15 минут.

## Что понадобится

- Аккаунт на [EdgeLab](https://platform.edgelab.su)
- AI-агент, который умеет делать HTTP-запросы
- 15 минут времени

## Шаг за шагом

<Steps>

<Step title="Зарегистрируйся на платформе">

Перейди на [platform.edgelab.su](https://platform.edgelab.su) и создай аккаунт. API доступен на всех тарифах, но объём функций зависит от уровня:

| Тариф | Доступ |
|---|---|
| **Edge** | Knowledge (read-only), профиль, поддержка |
| **Pro** | Всё из Edge + Network, Events, Contributions |
| **VIP** | Всё из Pro + управление событиями, созвон с основателем |

</Step>

<Step title="Получи API-ключ">

Зайди в **Настройки → API-ключ** и нажми «Создать ключ». Ключ выглядит так:

```
edgelab_live_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4
```

<Warning>
Ключ показывается только один раз при создании. Сохрани его сразу. Если потеряешь -- придётся создавать новый.
</Warning>

</Step>

<Step title="Добавь ключ в конфиг агента">

Ключ передаётся в заголовке `Authorization: Bearer ...` каждого запроса. Вот как добавить его в разные системы:

<CodeGroup>

```bash OpenClaw (переменная окружения)
# В ~/.zshrc или ~/.bashrc
export EDGELAB_API_KEY="edgelab_live_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4"

# Или в .env файле проекта
echo 'EDGELAB_API_KEY=edgelab_live_...' >> .env
```

```json Claude Code (MCP config)
{
  "mcpServers": {
    "edgelab": {
      "command": "npx",
      "args": ["edgelab-mcp"],
      "env": {
        "EDGELAB_API_KEY": "edgelab_live_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4"
      }
    }
  }
}
```

```json Cursor (.cursor/mcp.json)
{
  "mcpServers": {
    "edgelab": {
      "command": "npx",
      "args": ["edgelab-mcp"],
      "env": {
        "EDGELAB_API_KEY": "edgelab_live_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4"
      }
    }
  }
}
```

</CodeGroup>

<Tip>
Подробнее о безопасном хранении ключей -- в гайде [Безопасность API-ключей](/guides/api-key-security).
</Tip>

</Step>

<Step title="Проверь подключение: первый запрос">

Сделай `GET /knowledge/list` -- если ответ пришёл, всё работает:

<CodeGroup>

```bash curl
curl -X GET https://platform.edgelab.su/api/v1/knowledge/list \
  -H "Authorization: Bearer $EDGELAB_API_KEY"
```

```python Python
import requests
import os

API_KEY = os.environ["EDGELAB_API_KEY"]
BASE_URL = "https://platform.edgelab.su/api/v1"

response = requests.get(
    f"{BASE_URL}/knowledge/list",
    headers={"Authorization": f"Bearer {API_KEY}"}
)

data = response.json()
print(f"Статус: {'OK' if data['ok'] else 'Ошибка'}")
print(f"Материалов: {len(data['data'])}")
```

</CodeGroup>

Успешный ответ:

```json
{
  "ok": true,
  "data": [
    {
      "id": "knw_abc123",
      "title": "Введение в AI-агентов",
      "type": "lesson",
      "tier_required": "edge"
    },
    {
      "id": "knw_def456",
      "title": "Scout Agent Pattern",
      "type": "guide",
      "tier_required": "edge"
    }
  ],
  "meta": {
    "request_id": "req_xyz789",
    "api_version": "v1",
    "total": 2
  }
}
```

Если получил `401` -- проверь ключ. Если `403` -- возможно, endpoint недоступен на твоём тарифе.

</Step>

</Steps>

## Основные endpoints для агента

После подключения агент может использовать пять групп endpoints:

### Knowledge -- читать материалы

```bash
# Список всех материалов
GET /knowledge/list

# Детали конкретного материала
GET /knowledge/{id}

# Поиск по ключевым словам
GET /knowledge/search?q=scout+pattern
```

### Events -- смотреть события

```bash
# Ближайшие события (Pro+)
GET /events

# Детали события
GET /events/{id}

# Зарегистрироваться
POST /events/{id}/register
```

### Network -- искать участников

```bash
# Список участников (Pro+)
GET /network/members

# Профиль участника
GET /network/members/{id}

# Запрос на нетворкинг
POST /network/connect
```

### Support -- создать тикет

```bash
# Создать тикет
POST /support/tickets

# Посмотреть свои тикеты
GET /support/tickets
```

### Contributions -- делать вклад

```bash
# Отправить contribution (Pro+)
POST /contributions/submit

# Посмотреть свои contributions
GET /contributions/my
```

## Паттерн работы: поиск → детали → действие

Большинство задач агента следуют одному паттерну:

<Steps>

<Step title="Поиск">
Агент ищет нужные данные через `list` или `search` endpoint.
</Step>

<Step title="Детали">
Получает подробности по конкретному `id`.
</Step>

<Step title="Действие">
Выполняет действие: регистрация, подключение, отправка.
</Step>

</Steps>

Пример -- найти и зарегистрироваться на событие:

```python
import requests
import os

API_KEY = os.environ["EDGELAB_API_KEY"]
BASE_URL = "https://platform.edgelab.su/api/v1"
HEADERS = {"Authorization": f"Bearer {API_KEY}"}

# 1. Поиск -- список событий
events = requests.get(f"{BASE_URL}/events", headers=HEADERS).json()

# 2. Детали -- первое предстоящее событие
event = events["data"][0]
event_details = requests.get(
    f"{BASE_URL}/events/{event['id']}", headers=HEADERS
).json()
print(f"Событие: {event_details['data']['title']}")
print(f"Дата: {event_details['data']['starts_at']}")

# 3. Действие -- регистрация
result = requests.post(
    f"{BASE_URL}/events/{event['id']}/register", headers=HEADERS
).json()
print(f"Регистрация: {'OK' if result['ok'] else result['error']['message']}")
```

## Обработка ошибок

Агент должен корректно обрабатывать ошибки API:

```python
def api_request(method, path, **kwargs):
    """Обёртка с обработкой ошибок."""
    response = requests.request(
        method, f"{BASE_URL}{path}",
        headers=HEADERS, **kwargs
    )

    if response.status_code == 429:
        retry = int(response.headers.get("Retry-After", 60))
        return {"error": f"Rate limit. Повтори через {retry}с."}

    if response.status_code == 401:
        return {"error": "Невалидный API-ключ."}

    if response.status_code == 403:
        return {"error": "Нет доступа. Проверь тариф."}

    return response.json()
```

<Tip>
Rate limit -- 60 запросов в минуту. Для большинства агентов этого хватает с запасом. Если агент делает запросы в цикле -- добавь паузу 1 секунду между запросами.
</Tip>

## Что дальше

<CardGroup cols={2}>
  <Card title="Scout Pattern" icon="shield" href="/guides/scout-pattern">
    Двухуровневая защита от prompt injection
  </Card>
  <Card title="Contributions" icon="pen" href="/guides/contributions">
    Как стать контрибьютором
  </Card>
  <Card title="API Reference" icon="book" href="/api-reference/introduction">
    Все endpoints с примерами
  </Card>
  <Card title="Безопасность ключей" icon="lock" href="/guides/api-key-security">
    Хранение, ротация, отзыв
  </Card>
</CardGroup>
